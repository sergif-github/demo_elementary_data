services:
  postgres:
    image: postgres:15
    container_name: elementary_postgres
    environment:
      POSTGRES_DB: temp_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d admin"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:7
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8081:80"
    depends_on:
      - postgres
    restart: unless-stopped

  temp_data_ingestor:
    build: ./ingestor
    container_name: temp_data_ingestor
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: temp_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_PORT: 5432
      INGEST_INTERVAL_SECONDS: 300
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
